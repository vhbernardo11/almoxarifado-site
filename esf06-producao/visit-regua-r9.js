(()=>{'use strict';
if(window.__ESF06_VISIT_REGUA_R9__)return;window.__ESF06_VISIT_REGUA_R9__=true;
const BASE='https://pvwqxpqetdxtmqqypqjk.supabase.co/functions/v1/';
const ACCESS=new URL(location.href).searchParams.get('k')||localStorage.getItem('esf06_visit_key')||'';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt=s=>{if(!s)return '—';const m=String(s).match(/^(\d{4})-(\d{2})-(\d{2})/);return m?`${m[3]}/${m[2]}/${m[1]}`:esc(s)};
const statusLabel=s=>({atrasado:'Atrasado',prioritario:'Prioritário',janela:'Janela de visita',referencia_pendente:'Referência pendente',acompanhar:'Acompanhar',individualizado:'Planejamento individualizado',em_dia:'Em dia'}[s]||s||'—');
const sourceLabel=s=>s==='federal'?'Regra federal':s==='operacional_local'?'Regra operacional ESF06':'Conforme necessidade';

const style=document.createElement('style');style.id='visit-regua-r9-style';style.textContent=`
.regua-fab{position:fixed;right:14px;bottom:164px;z-index:78;border:0;border-radius:999px;background:#071a31;color:#fff;padding:11px 14px;font-weight:900;box-shadow:0 8px 28px #0015;display:flex;align-items:center;gap:7px;min-height:46px}.regua-fab .badge{min-width:22px;height:22px;padding:0 6px;border-radius:999px;background:#cf3f45;display:grid;place-items:center;font-size:11px}.regua-back{position:fixed;inset:0;z-index:650;background:#071a31bb;display:flex;align-items:flex-end;justify-content:center;padding:10px}.regua-modal{width:min(100%,760px);max-height:94vh;overflow:auto;background:#f5f8fb;border-radius:24px 24px 18px 18px;box-shadow:0 22px 70px #0018}.regua-head{position:sticky;top:0;z-index:2;background:linear-gradient(120deg,#071a31,#0d2b4e);color:#fff;padding:16px;border-radius:24px 24px 0 0}.regua-head-top{display:flex;align-items:flex-start;gap:10px}.regua-head h2{margin:0;font-size:22px}.regua-head p{margin:5px 42px 0 0;color:#c9d5e4;font-size:12px;line-height:1.4}.regua-close{margin-left:auto;border:0;border-radius:50%;width:42px;height:42px;background:#ffffff18;color:#fff;font-size:21px}.regua-body{padding:12px}.regua-kpis{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-bottom:10px}.regua-kpi{background:#fff;border:1px solid #dfe6ed;border-radius:15px;padding:10px}.regua-kpi b{font-size:22px;display:block}.regua-kpi span{font-size:10px;color:#708096}.regua-tools{display:grid;grid-template-columns:1fr 170px;gap:8px;margin-bottom:10px}.regua-tools input,.regua-tools select{border:1px solid #d9e1e9;border-radius:13px;padding:11px;background:#fff;min-height:44px}.regua-card{background:#fff;border:1px solid #dfe6ed;border-left:6px solid #708096;border-radius:17px;padding:12px;margin-bottom:9px}.regua-card.atrasado{border-left-color:#cf3f45}.regua-card.prioritario{border-left-color:#d46d00}.regua-card.janela{border-left-color:#9a7100}.regua-card.em_dia{border-left-color:#218a5d}.regua-card.referencia_pendente,.regua-card.individualizado,.regua-card.acompanhar{border-left-color:#246bfd}.regua-card-head{display:flex;gap:8px;align-items:flex-start}.regua-name{font-weight:900;font-size:16px}.regua-meta{font-size:11px;color:#708096;line-height:1.45;margin-top:3px}.regua-status{margin-left:auto;border-radius:999px;padding:5px 8px;font-size:10px;font-weight:900;background:#eef2f6;white-space:nowrap}.regua-card.atrasado .regua-status{background:#ffe5e5;color:#9d3030}.regua-card.prioritario .regua-status{background:#fff0d9;color:#935300}.regua-card.janela .regua-status{background:#fff7dd;color:#7d631f}.regua-card.em_dia .regua-status{background:#eaf9f2;color:#216c4e}.regua-track{margin-top:9px;padding:9px;border-radius:12px;background:#f7f9fb;border:1px solid #e8edf2}.regua-track-top{display:flex;gap:7px;align-items:center;flex-wrap:wrap}.regua-track b{font-size:12px}.regua-pill{padding:4px 6px;border-radius:999px;background:#eaf2ff;color:#2b5f9e;font-size:9px;font-weight:900}.regua-track p{margin:5px 0 0;font-size:11px;color:#5f6e82;line-height:1.45}.regua-alert{margin-top:8px;padding:8px 9px;border-radius:11px;background:#fff2e8;color:#8a4f13;font-size:11px;font-weight:800;line-height:1.4}.regua-alert.red{background:#fff0f0;color:#a52f35}.regua-sources{margin:12px 0;background:#fff;border:1px solid #dfe6ed;border-radius:15px;padding:10px}.regua-sources summary{font-weight:900;cursor:pointer}.regua-source{padding:8px 0;border-bottom:1px solid #edf1f4;font-size:11px;line-height:1.45}.regua-source:last-child{border-bottom:0}.regua-source a{color:#1857ad;font-weight:850}.regua-empty{padding:24px;text-align:center;color:#708096}.regua-foot{font-size:10px;color:#708096;line-height:1.45;margin:10px 2px 18px}.regua-loading{padding:28px;text-align:center;font-weight:850;color:#526176}
@media(min-width:720px){.regua-back{align-items:center}.regua-modal{border-radius:24px}.regua-kpis{grid-template-columns:repeat(6,minmax(0,1fr))}}@media(max-width:520px){.regua-tools{grid-template-columns:1fr}.regua-kpis{grid-template-columns:repeat(2,minmax(0,1fr))}.regua-fab{bottom:158px;right:10px;padding:10px 12px}}
`;document.head.appendChild(style);

const btn=document.createElement('button');btn.type='button';btn.className='regua-fab';btn.innerHTML='<span>⏱</span><span>Régua ACS</span><span class="badge" hidden>0</span>';document.body.appendChild(btn);
let cache=null,lastLoad=0;
async function load(force=false){if(!force&&cache&&Date.now()-lastLoad<60000)return cache;if(!ACCESS)throw new Error('Chave de acesso não encontrada');const r=await fetch(`${BASE}acs-visit-regua?api=list&k=${encodeURIComponent(ACCESS)}`,{cache:'no-store'});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Falha ao carregar régua');cache=j;lastLoad=Date.now();const n=(j.summary?.prioritarios||0)+(j.summary?.janela||0);const badge=btn.querySelector('.badge');if(badge){badge.textContent=String(n);badge.hidden=!n}return j}
async function refreshBadge(){try{await load(true)}catch(e){console.warn('visit-regua',e)}}

function trackHtml(t){const details=[];if(t.phase)details.push(esc(t.phase));if(t.next_due)details.push(`Próximo marco: <b>${fmt(t.next_due)}</b>`);if(t.last_visit)details.push(`Última visita: ${fmt(t.last_visit)}`);if(t.federal_compliant_operational===true)details.push('Critério federal atendido operacionalmente');if(t.federal_compliant_operational===false)details.push('Critério federal ainda não completo na janela móvel');if(t.note)details.push(esc(t.note));return `<div class="regua-track"><div class="regua-track-top"><b>${esc(t.label||t.rule_key)}</b><span class="regua-pill">${esc(sourceLabel(t.source_kind))}</span><span class="regua-pill">${esc(statusLabel(t.status))}</span></div><p>${details.join(' · ')||'Acompanhamento conforme regra cadastrada.'}</p></div>`}
function cardHtml(x){return `<article class="regua-card ${esc(x.overall_status)}" data-regua-status="${esc(x.overall_status)}" data-regua-search="${esc((x.full_name+' '+(x.family_code||'')+' '+(x.address||'')).toLowerCase())}"><div class="regua-card-head"><div><div class="regua-name">${esc(x.full_name)}</div><div class="regua-meta">Família ${esc(x.family_code||'—')} · ${esc(x.address||'Endereço não informado')}<br>${x.next_due?`Próximo prazo: <b>${fmt(x.next_due)}</b>`:'Sem prazo numérico fixo'}</div></div><span class="regua-status">${esc(statusLabel(x.overall_status))}</span></div>${(x.tracks||[]).map(trackHtml).join('')}${x.registration_pending?'<div class="regua-alert">⚠ Há visita operacional, mas o conjunto de registros oficiais ainda não comprova todos os critérios federais.</div>':''}${x.newborn_registration_needed?'<div class="regua-alert red">👶 RN ainda não localizado no cadastro desta família. Cadastre a criança para ativar automaticamente a régua infantil.</div>':''}</article>`}
function sourcesHtml(rules){const seen=new Set();return (rules||[]).filter(r=>{const k=r.rule_key;if(seen.has(k))return false;seen.add(k);return true}).map(r=>`<div class="regua-source"><b>${esc(r.label)}</b> · ${esc(sourceLabel(r.source_kind))}<br>${esc(r.notes||'')} ${r.source_url&&/^https:/.test(r.source_url)?`<br><a href="${esc(r.source_url)}" target="_blank" rel="noopener noreferrer">Abrir fonte</a>`:''}</div>`).join('')}
function applyFilter(root){const q=(root.querySelector('[data-regua-q]')?.value||'').trim().toLowerCase();const f=root.querySelector('[data-regua-filter]')?.value||'acao';root.querySelectorAll('.regua-card').forEach(c=>{const s=c.dataset.reguaStatus||'',txt=c.dataset.reguaSearch||'';const okQ=!q||txt.includes(q);const okF=f==='all'||(f==='acao'?['atrasado','prioritario','janela'].includes(s):f==='individualizado'?['individualizado','referencia_pendente','acompanhar'].includes(s):s===f);c.hidden=!(okQ&&okF)})}
async function open(){const back=document.createElement('div');back.className='regua-back';back.innerHTML='<section class="regua-modal"><div class="regua-head"><div class="regua-head-top"><div><h2>Régua Oficial de Visitas ACS</h2><p>Regras federais separadas das metas operacionais da ESF06. Os mínimos nunca substituem o planejamento por risco e necessidade.</p></div><button class="regua-close" type="button" aria-label="Fechar">×</button></div></div><div class="regua-body"><div class="regua-loading">Calculando acompanhamento…</div></div></section>';document.body.appendChild(back);const close=()=>back.remove();back.querySelector('.regua-close').onclick=close;back.onclick=e=>{if(e.target===back)close()};try{const j=await load(true),s=j.summary||{};const body=back.querySelector('.regua-body');body.innerHTML=`<div class="regua-kpis"><div class="regua-kpi"><b>${s.prioritarios||0}</b><span>prioritários / atrasados</span></div><div class="regua-kpi"><b>${s.janela||0}</b><span>na janela</span></div><div class="regua-kpi"><b>${s.em_dia||0}</b><span>em dia</span></div><div class="regua-kpi"><b>${s.individualizados||0}</b><span>individualizados</span></div><div class="regua-kpi"><b>${s.registro_oficial_pendente||0}</b><span>registro oficial pendente</span></div><div class="regua-kpi"><b>${s.rn_cadastro_pendente||0}</b><span>RN a cadastrar</span></div></div><div class="regua-tools"><input data-regua-q type="search" placeholder="Buscar pessoa, família ou endereço"><select data-regua-filter><option value="acao">Precisa de ação</option><option value="all">Todos</option><option value="atrasado">Atrasados</option><option value="prioritario">Prioritários</option><option value="janela">Janela de visita</option><option value="em_dia">Em dia</option><option value="individualizado">Planejamento individualizado</option></select></div><div data-regua-list>${(j.items||[]).map(cardHtml).join('')||'<div class="regua-empty">Nenhuma pessoa enquadrada nas regras atuais.</div>'}</div><details class="regua-sources"><summary>Regras e fontes cadastradas</summary>${sourcesHtml(j.rules)}</details><div class="regua-foot">Atualizado em ${fmt(j.today)}. A régua usa as visitas realizadas no Gestão ACS 360 para planejamento operacional e mostra separadamente quando os registros oficiais ainda não completam os critérios. Gestantes sem data estruturada da primeira consulta de pré-natal são sinalizadas, sem inventar prazo.</div>`;body.querySelector('[data-regua-q]').oninput=()=>applyFilter(body);body.querySelector('[data-regua-filter]').onchange=()=>applyFilter(body);applyFilter(body)}catch(e){back.querySelector('.regua-body').innerHTML=`<div class="regua-empty"><b>Não foi possível carregar a régua.</b><br>${esc(e.message||e)}</div>`}}
btn.onclick=open;
setTimeout(refreshBadge,1800);
})();